let num: number[] = new Array(6);
num = [20, 14, 8, 0, 5, 19, 24];
for (var i = 0; i < num.length; i++) {
    let element: number = num[i];
    console.log("El numero en la posicion " + i + " es " + element);
}